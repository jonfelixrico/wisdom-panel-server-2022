import { SessionUserDiscordApiClientMiddleware } from './session-user-discord-api-client.middleware'

describe('SessionUserDiscordApiClientMiddleware', () => {
  it('should be defined', () => {
    expect(new SessionUserDiscordApiClientMiddleware()).toBeDefined()
  })
})
